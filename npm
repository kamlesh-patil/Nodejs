Learned from Brad Traversy from youtube channel Traversy Media

# GET VERSION
npm -v (or --version)

# GET HELP
npm help
npm

# CREATE PACKAGE.JSON
npm init
npm init -y (or --yes)

# SET DEFAULTS
npm config set init-author-name "YOUR NAME"       config is option here and set means setting
npm set init-license "MIT"

# GET DEFAULTS
npm config get init-author-name
npm get init-license

# REMOVE DEFAULTS
npm config delete init-author-name
npm delete init-license

# INSTALLING LOCAL PACKAGES
npm install lodash --save (or npm install --save lodash)
npm install moment --save
npm install gulp gulp-sass --save-dev

# MOVE TO ANOTHER FOLDER
npm install                     it will install all the packages
npm install --production        it will install only few necessary packages like lodash

# REMOVING MODULES
npm uninstall gulp-sass --save-dev
npm remove gulp --save-dev

#INSTALL CERTAIN VERSIONS
npm install lodash@4.17.3 --save

# UPDATE
npm update lodash --save


# FIND ROOT FOLDER
npm root -g


# LISTING PACKAGES
npm list
npm list --depth 0    it will list only main packages not their dependencies
npm list --depth 1    it will list packages dependencies upto 1 more level i.e. sub packages
